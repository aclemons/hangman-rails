<% provide(:title, t("ALL_GAMES_TITLE")) %>
<h1><%= t("ALL_GAMES") %></h1>

<%= will_paginate %>

<table class="table table-striped">
  <tr>
    <th><%= t("OVERVIEW_GAME_ID") %></th>
    <th><%= t("OVERVIEW_GAME_STATUS") %></th>
    <th><%= t("OVERVIEW_GAME_LIVES_REMAINING") %></th>
    <th><%= t("OVERVIEW_GAME_LINK") %></th>
  </tr>
  <%= render @games %>
</table>

<%= will_paginate %>

